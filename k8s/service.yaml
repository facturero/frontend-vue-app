apiVersion: v1
kind: Service
metadata:
  name: crm-frontend
  labels:
    app: crm-frontend
spec:
  type: NodePort
  selector:
    app: crm-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
